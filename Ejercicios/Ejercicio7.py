# Ejercicio 7 — Top 5 estados con más ventas (en dinero)
# Agrupa por State, suma Sales, ordena de mayor a menor y quédate con los 5 primeros. 
# Aquí tienes que encadenar tres cosas: groupby + sort_values(ascending=False) + head(5). 
# Es como un GROUP BY + ORDER BY DESC + LIMIT 5 de SQL, todo seguido.